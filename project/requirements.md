#There is a need to create a text file which contains the book of Genesis.
##By using the utility commands (sort, awk, grep, unique ) I will be able to
###count how many times each letter of the alphabet is used in the file.

